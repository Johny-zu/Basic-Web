# Funcionalidad
En la parte funcional del codigo este se ingresa con un entrelace de elementos ID, dando un enlace desde el documento

cuando se tiene todas las variables preparadas se presenta la funcion de procesar el formulario, donde cargan los valores delos inputs, revisar que se recolecten los datos y usamos una variable polarizada para ir metiendo los valores de manera directa en base a lo captruado.

Despues de esto realizamos un ineer para mostrar un div oculto desde el CSS para que este al pulsar el boton del enviar pueda enviar el formulario 1 en un div con el fin de mostrar los mensajes

Al final el boton crea un evento de click que procesa una funcion